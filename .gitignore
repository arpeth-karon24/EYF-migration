# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
.env
.env*.local

# cloudflare
.wrangler/
.vercel/output/

# typescript
*.tsbuildinfo
next-env.d.ts

# reference captures (optional)
_source-home.html

# Local reference docs (planning, migration notes) — never commit
/reference_docs/

# Claude Code internal files — keep on disk only, never push to GitHub
/.claude/

# Sanity Studio runtime cache — never push
/.sanity/

# Internal backend/planning docs — never push
/docs/

# Root README only; other markdown at repo root stays local
/*.md
!README.md
